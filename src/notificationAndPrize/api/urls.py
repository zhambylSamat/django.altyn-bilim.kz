from django.urls import path
# from .views import CheckStudentAttendanceNotification, CheckTopicQuizNotification, CheckTrialTestNotification

urlpatterns = [
    # path('run/abs/notification/', CheckStudentAttendanceNotification.as_view()),
    # path('run/quiz/notification/', CheckTopicQuizNotification.as_view()),
    # path('run/trial/test/notification/', CheckTrialTestNotification.as_view()),
    # path('fetch/', FetchExistingMaterials.as_view()),
]
